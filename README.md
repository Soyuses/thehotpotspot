# 🍔 The Hot Pot Spot - Blockchain Restaurant Management System

Система управления рестораном на блокчейне с токенами, голосованием и API для внешних систем.

## 🚀 Возможности

### Универсальный API
- **GET /menu** - Получение полного прейскуранта
- **GET /menu/{id}** - Получение информации о конкретной позиции
- **POST /order** - Создание заказа
- **POST /order/cancel** - Отмена заказа
- **GET /order/{id}/status** - Статус заказа
- **GET /wallet/{address}/balance** - Баланс кошелька

### Прейскурант
- ✅ Название и описание блюда
- ✅ Цена
- ✅ Доступность (количество штук)
- ✅ Приоритетность (ранк 1-10)
- ✅ Время готовки в минутах
- ✅ Полный состав с ингредиентами
- ✅ Количество каждого ингредиента в граммах
- ✅ Калорийность каждого ингредиента и общая

### Система заказов
- ✅ Создание заказа с указанием блюд и количества
- ✅ Время доставки (когда может приехать курьер)
- ✅ Кошелек покупателя
- ✅ Статусы: на рассмотрении, подтвержден, отменен
- ✅ Автоматическое начисление токенов при подтверждении
- ✅ Отмена заказа с указанием причины

### Смарт-контракты
- ✅ **Покупка**: Процедуры покупки реализованы через блокчейн-логику
- ✅ **Голосование**: Система голосования за новые блюда с utility токенами
- ✅ **Токены**: Security токены (1:1 к сумме покупки) и Utility токены (10% для голосования)

## 🏗️ Архитектура

### Основные компоненты

1. **Blockchain** - Основная блокчейн логика
2. **MenuItem** - Позиции меню с полной информацией
3. **Order** - Система заказов
4. **TokenHolder** - Управление токенами и ролями
5. **ApiServer** - HTTP API сервер
6. **UI** - Консольный интерфейс

### Роли пользователей

- **MainOwner** - Владелец ресторана (может добавлять меню, подтверждать заказы)
- **BigStack** - Большие держатели токенов (могут предлагать новые блюда)
- **MiddlePlayer** - Средние держатели (5-10% токенов)
- **Starter** - Начинающие держатели (1-5% токенов)
- **Unauthorized** - Пользователи без токенов

## 🖥️ Интерфейсы

### 1. Интерфейс владельца ресторана (`restaurant_owner.html`)

**Функции:**
- ✅ **Ручной ввод данных в прейскурант** - табличная форма для добавления блюд
- ✅ **Выделение блюд для голосования** - новые позиции становятся доступными для голосования
- ✅ **Просмотр заказов** - отдельная табличная форма со всеми заказами
- ✅ **Подтверждение заказов** - запись в блокчейн, начисление токенов покупателю

### 2. Интерфейс кошелька (`wallet_interface.html`)

**Функции:**
- ✅ **Смена темы** - кнопка в правом нижнем углу для смены фона
- ✅ **Запрос прейскуранта** - кнопка обновления актуального меню
- ✅ **Отображение прейскуранта** - полная информация о блюдах
- ✅ **Выбор и заказ блюд** - указание количества и оформление заказа
- ✅ **Статус заказа** - отображение статуса (на рассмотрении/подтвержден)
- ✅ **Баланс токенов** - отображение всех полученных токенов

## 🛠️ Установка и запуск

### Требования
- Rust 1.70+
- Cargo

### Установка зависимостей
```bash
cd blockchain_project
cargo build
```

### Запуск
```bash
cargo run
```

### Запуск различных режимов

#### 1. API сервер (базовый)
```bash
$env:API_ONLY=1; cargo run
# Сервер на порту 3000
```

#### 2. Франшизная сеть API
```bash
$env:FRANCHISE_API=1; cargo run
# API на порту 3001 с демонстрацией токеномики
```

#### 3. P2P сеть
```bash
$env:P2P_NETWORK=1; cargo run
# P2P узел с консенсусом Proof of Sales + Reputation
```

#### 4. Полная децентрализованная сеть
```bash
$env:FULL_DECENTRALIZED=1; cargo run
# P2P + IPFS + Консенсус + Смарт-контракты
```

#### 5. Консольный интерфейс
```bash
cargo run
# Интерактивное меню с опциями
```

### Открытие веб-интерфейсов
1. **Владелец ресторана**: Откройте `restaurant_owner.html` в браузере
2. **Кошелек покупателя**: Откройте `wallet_interface.html` в браузере

**Примечание**: 
- API сервер: порт 3000 (http://localhost:3000)
- Франшизная сеть: порт 3001 (http://localhost:3001)
- P2P сеть: порт 8080 (http://localhost:8080)

## 📡 API Примеры

### Получение меню
```bash
curl -X POST http://localhost:3000 \
  -H "Content-Type: application/json" \
  -d '{"GetMenu": {}}'
```

### Создание заказа
```bash
curl -X POST http://localhost:3000 \
  -H "Content-Type: application/json" \
  -d '{
    "CreateOrder": {
      "customer_wallet": "Customer_John",
      "items": [
        {"menu_item_id": "burger1", "quantity": 2},
        {"menu_item_id": "pizza1", "quantity": 1}
      ],
      "delivery_time_minutes": 30
    }
  }'
```

### Отмена заказа
```bash
curl -X POST http://localhost:3000 \
  -H "Content-Type: application/json" \
  -d '{
    "CancelOrder": {
      "order_id": "ORDER_12345678",
      "reason": "Изменение планов",
      "customer_wallet": "Customer_John"
    }
  }'
```

### Получение баланса кошелька
```bash
curl -X POST http://localhost:3000 \
  -H "Content-Type: application/json" \
  -d '{
    "GetWalletBalance": {
      "wallet": "Customer_John"
    }
  }'
```

## 🔧 Технические детали

### Структуры данных

#### MenuItem
```rust
struct MenuItem {
    id: String,
    name: String,
    description: String,
    price: f64,
    availability: u32,
    priority_rank: u32,
    cooking_time_minutes: u32,
    ingredients: Vec<Ingredient>,
    total_calories: f64,
    // ... голосование и статус
}
```

#### Order
```rust
struct Order {
    id: String,
    customer_wallet: String,
    items: Vec<OrderItem>,
    total_amount: f64,
    delivery_time_minutes: u32,
    status: OrderStatus,
    tokens_issued: f64,
    // ... временные метки
}
```

### Токеномика

- **Security Tokens**: 1:1 к сумме покупки, дают право на майнинг и роли
- **Utility Tokens**: 10% от суммы покупки, используются для голосования
- **Роли**: Определяются процентом от общего количества Security токенов

### Блокчейн

- **Proof of Stake**: Валидаторы выбираются по количеству Security токенов
- **Майнинг**: Награда за майнинг блока
- **Транзакции**: Все операции записываются в блокчейн

## 🎯 Демонстрация

При запуске система автоматически:
1. Создает примеры меню с полной информацией
2. Генерирует тестовые покупки и чеки
3. Создает пример заказа
4. Майнит несколько блоков
5. Запускает консольный интерфейс

## 🔮 Будущие улучшения

- [ ] Веб-интерфейс для API
- [ ] База данных для персистентности
- [ ] Мобильное приложение
- [ ] Интеграция с реальными платежными системами
- [ ] Расширенная система ролей
- [ ] Аналитика и отчеты

## 📄 Лицензия

MIT License

## 👥 Авторы

Разработано как демонстрация блокчейн-системы для ресторанного бизнеса.

## 🏪 The Hot Pot Spot

Этот проект представляет собой полнофункциональную систему управления рестораном "The Hot Pot Spot" с использованием блокчейн-технологий.
